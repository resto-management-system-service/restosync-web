import { Heading } from 'restosync-web';

export const Default = () => (
  <Heading title='Menu items' description='Create, edit and organise the dishes on your menu.' />
);

export const Section = () => (
  <Heading title='Categories' description='6 categories · drag to reorder.' />
);
